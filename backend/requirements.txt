# Hey Havi API — Backend dependencies
# Instalación: pip install -r requirements.txt
# Levantar:    uvicorn main:app --reload --port 8000

fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.3
pandas==2.3.3
numpy==2.2.6
python-dotenv>=1.0.0

# ── LLM ───────────────────────────────────────────────────────
# Ollama (local, sin costo) ← DEFAULT
# Requiere instalar Ollama: https://ollama.com
# y descargar el modelo:    ollama pull gemma4:e2b
ollama>=0.4.0
requests>=2.32.0   # para verificar si Ollama está corriendo

# Anthropic (Claude en la nube):
# anthropic>=0.40.0

# OpenAI (GPT en la nube):
# openai>=1.50.0
